# yatube_project

## Описание

Yatube — это блог-платформа и социальная сеть, где пользователи могут публиковать посты, подписываться на авторов и взаимодействовать с контентом.

Проект находится в стадии активной разработки и будет дополняться по мере его развития.
Сейчас проект также разворачивается на PythonAnywhere.

## Функциональность

- **Административная панель Django**:

  - создание и редактирование пользователей и групп;
  - создание, редактирование и удаление постов;
  - привязка постов к группам при создании или редактировании;
  - загрузка изображений к постам (поддержка через `sorl.thumbnail`).

- **Пользовательская часть**:

  - главная страница со списком постов (с постраничной навигацией);
  - страницы групп с постами соответствующей группы;
  - профили пользователей с их постами;
  - детальный просмотр поста;
  - создание и редактирование постов доступно только авторизованным пользователям;
  - возможность комментирования постов авторизованными пользователями;
  - адаптивное навигационное меню, которое меняется в зависимости от авторизации пользователя;
  - кастомные страницы ошибок: 403, 403csrf, 404, 500.

- **Регистрация и авторизация**:

  - регистрация нового пользователя;
  - авторизация и выход из системы;
  - смена и сброс пароля.

- **Подписки и фильтры**:

  - подписка и отписка на авторов;
  - фильтр просмотра постов на главной странице: все посты / посты избранных авторов.

- **Шаблоны и интерфейс**:

  - базовый шаблон `base.html`, включающий `header.html` и `footer.html`;
  - текущий год в футере отображается с помощью контекст-процессора `year`;
  - адаптивное меню с использованием JS и Bootstrap.

- **Дополнительно**:

  - постраничная навигация через объект `Paginator`;
  - эмуляция почтового сервера через `EmailBackend`, письма сохраняются в директории `sent_emails`.

- **Статические страницы**:

  - об авторе;
  - технологии.

## Технологии

- Python 3.14
- Django 6.0
- SQLite
- Bootstrap, JavaScript (для адаптивного меню)

## Структура проекта

- **Приложения**:

  - `posts` — управление постами, группами, комментариями и изображениями;
  - `users` — регистрация, авторизация, профили пользователей;
  - `core` — пользовательские фильтры и вспомогательные функции;
  - `about` — статические страницы «об авторе» и «технологии».

- **Шаблоны**:

  - `base.html`, `header.html`, `footer.html`, `field_in_form.html`;
  - `index.html`, `group_list.html`, `profile.html`, `post_detail.html`, `create_post.html` и др.

- **Статика**:

  - CSS, JS и изображения лежат в папке `static/`.

## Тестирование

В проекте реализованы автоматические тесты с использованием встроенного тестового фреймворка Django.

Покрытие тестами:

- приложение `about`: views, urls;
- приложение `users`: views, urls, forms;
- приложение `posts`: views, urls, forms, models.

Для запуска тестов выполните команду:

```bash
python manage.py test
```

## Запуск проекта в dev-режиме

1. Клонируйте репозиторий:

    ```bash
    git clone https://github.com/whymello/yatube_project.git
    ```

2. Перейдите в папку проекта и создайте виртуальное окружение:

    ```bash
    python -m venv venv
    source venv/bin/activate  # для GNU/Linux, macOS
    venv\Scripts\activate     # для Windows
    ```

3. Установите зависимости:

    ```bash
    pip install -r requirements.txt
    ```

4. Перейдите в папку `yatube` и выполните миграции:

    ```bash
    python manage.py migrate
    ```

5. Создайте суперпользователя (по желанию):

    ```bash
    python manage.py createsuperuser
    ```

6. Запустите сервер разработки:

    ```bash
    python manage.py runserver
    ```

    Проект будет доступен по адресу: <http://127.0.0.1:8000/>

## Автор

Galiullin Azat
